refactor: Adjust spacing and margins for info pages

- Đã điều chỉnh khoảng cách và lề cho `HelpPage.vue`, `OnboardingPage.vue`, và `VersionPage.vue` để cải thiện bố cục và khả năng đọc.
- Đã giảm `margin-top` của các `v-row` từ `mt-6` xuống `mt-3` trong các trang này.
- Đã loại bỏ `class="pa-4"` khỏi các `v-card` trong các trang này để giảm padding.
- Đã thay đổi kích thước font của các đoạn văn mô tả từ `text-body-2` thành `text-body-1` trong các trang này.